{
  "name": "@fanon/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "eslint .",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "hono": "^4.12.24",
    "prisma": "^7.8.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@fanon/eslint-config": "workspace:*",
    "@fanon/typescript-config": "workspace:*",
    "eslint": "^9.0.0"
  }
}
